---
description: Tool-agnostic wrapper. Verify a batch using docs/agent-commands/batch-verify.md.
---

Read and follow:

```text
docs/agent-commands/batch-verify.md
```

Pass through `$ARGUMENTS` unchanged.
